# Maintainer: Ivan Bushchik <ivabus@plainde.org> and thm-unix 
<segfault@plainde.org>

pkgname="plainpanel-git"
pkgver="git"
pkgver=1
pkgdesc='plainDE panel'
arch=('x86_64', 'aarch64')
usr='https://plainde.org'
license=('GPL')
makedepends=('qt6-base', 'base-devel', 'noto-fonts-emoji', 'git')
optdepends=('openbox: window manager',
            'fluxbox: window manager')

provides=("${plainpanel-git}")
conflicts=("${plainpanel-git}", "plainpanel")
md5sums=('SKIP')
source=)

